{# @var admin \Prognoz\AdminBundle\Admin\TournamentAdmin #}
{% set property = admin.route('move').requirement('property') %}

{% if attribute(object, property) > 0 %}
    <a class="btn btn-small" href="{{ admin.generateObjectUrl('move', object, {'move': 'top', 'property': property}) }}" title="{{ 'move.top'|trans }}"><i class="icon icon-step-backward"></i></a>
{% endif %}

{% if attribute(object, property) > 0 %}
    <a class="btn btn-small" href="{{ admin.generateObjectUrl('move', object, {'move': 'up', 'property': property}) }}" title="{{ 'move.up'|trans }}"><i class="icon icon-arrow-up"></i></a>
{% endif %}

{% if attribute(object, property) < admin.getPositionService.getLastPosition(admin.getClass, property) %}
    <a class="btn btn-small" href="{{ admin.generateObjectUrl('move', object, {'move': 'down', 'property': property}) }}" title="{{ 'move.down'|trans }}"><i class="icon icon-arrow-down"></i></a>
{% endif %}

{% if attribute(object, property) < admin.getPositionService.getLastPosition(admin.getClass, property) %}
    <a class="btn btn-small" href="{{ admin.generateObjectUrl('move', object, {'move': 'bottom', 'property': property}) }}" title="{{ 'move.bottom'|trans }}"><i class="icon icon-step-forward"></i></a>
{% endif %}
